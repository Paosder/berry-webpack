/**
 * This object will be used for local dll.
 * Each key points local directory or file.
 * The served value must be unique to validate version(like checksum, always pure if function).
 * ex)
 * ```
 * {
 *  withStringTest: "hello world!",
 *  withFunctionTest: () => "hello world!"
 * }
 */

module.exports = {};
